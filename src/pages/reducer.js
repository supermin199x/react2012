const initialState = {
    data: [
        { id:1, name:'min', score:90},
        { id:2, name:'wiw', score:70},
        { id:3, name:'may', score:50},
    ]
}

const reducer = (state = initialState, action) => {
    return state;
}

export default reducer;